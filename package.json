{
  "name": "appecomerce",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "server.js",
    "start": "node server.js",
    "migrate": "sequelize-cli db:migrate --config src/config/config.js --migrations-path src/migrations",
    "migrate:undo": "sequelize-cli db:migrate:undo:all --config src/config/config.js --migrations-path src/migrations",
    "seed": "sequelize-cli db:seed:all --config src/config/config.js --seedrs-path src/seeders",
    "seed:undo": "sequelize-cli db:seed:undo:all --config src/config/config.js --seeders-path src/seeders",
    "migration:create": "sequelize-cli migration:generate --config src/config/config.js --migration-path src/migrations --name"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.22.0",
    "sequelize": "^6.37.8"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.5"
  }
}
